name: seeforme01
description: A new Flutter project for object detection
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0+4
  http: ^1.1.0
  path: ^1.8.3
  path_provider: ^2.1.1
  permission_handler: ^11.1.0
  flutter_tts: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
